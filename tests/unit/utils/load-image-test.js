import loadImage from 'wilks-bday/utils/load-image';
import { module, test } from 'qunit';

module('Unit | Utility | load-image', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = loadImage();
    assert.ok(result);
  });
});
